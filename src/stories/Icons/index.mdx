import { Meta } from '@storybook/blocks';

import * as IconsStories from './index.stories';

<Meta of={IconsStories} />

# Icons

All icons are exported as React Components. You can directly use
them as just any other React Component.
