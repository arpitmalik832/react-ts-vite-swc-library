const ENVS = {
  DEV: 'development',
  STG: 'staging',
  BETA: 'beta',
  PROD: 'production',
};

const THEME = {
  LIGHT: 'light',
  DARK: 'dark',
};

const API1_TIMEOUT = 15000;

export { ENVS, THEME, API1_TIMEOUT };
